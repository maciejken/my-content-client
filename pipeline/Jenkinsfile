pipeline {
    agent {
      // docker { image 'node:14-alpine' }
      node {
        label 'master'
      }
    }
    stages {
        stage('build') {
            steps {
                sh 'echo $USER'
                sh 'npm --version'
            }
        }
    }
}